{
  "name": "launcher",
  "scripts": {
    "dev": "rm -rf build && parcel src/index.tsx --public-url=/app/launcher --dist-dir build -p 12300",
    "build": "rm -rf build && parcel build src/index.tsx --public-url=/app/launcher --dist-dir build"
  },
  "dependencies": {
    "@types/react": "^18.0.27",
    "@types/react-dom": "^18.0.10",
    "csstype": "^3.1.1",
    "goober": "^2.1.11",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "process": "^0.11.10",
    "postcss": "^8.4.21",
    "tailwindcss": "^3.2.4"
  },
  "workspaces": [
    "../../core/rice"
  ]
}
